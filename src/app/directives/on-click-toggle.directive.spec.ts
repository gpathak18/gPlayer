import { OnClickToggleDirective } from './on-click-toggle.directive';

describe('OnClickToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new OnClickToggleDirective();
    expect(directive).toBeTruthy();
  });
});
