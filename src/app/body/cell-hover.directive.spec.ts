import { CellHoverDirective } from './cell-hover.directive';

describe('CellHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new CellHoverDirective();
    expect(directive).toBeTruthy();
  });
});
